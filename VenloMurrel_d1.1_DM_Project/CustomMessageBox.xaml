<Window x:Class="VenloMurrel_d1._1_DM_Project.CustomMessageBox"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:VenloMurrel_d1._1_DM_Project"
        mc:Ignorable="d"
        Title="CustomMessageBox" Height="200" Width="200" 
        AllowsTransparency="True" 
        Background="Transparent"
        WindowStyle="None" ResizeMode="NoResize">
    <Border CornerRadius="15" BorderBrush="Aquamarine" BorderThickness="1">
        <Border.Background>
            <LinearGradientBrush>
                <GradientStop Color="White" Offset="0.2"/>
                <GradientStop Color="LightBlue" Offset="0.6"/>
                <GradientStop Color="LightSkyBlue" Offset="1"/>
            </LinearGradientBrush>
        </Border.Background>
        <Grid>

            <Grid.RowDefinitions>
                <RowDefinition/>
                <RowDefinition/>
            </Grid.RowDefinitions>
            <Label x:Name="lblInhoud" Grid.Row="0" HorizontalAlignment="Center" VerticalAlignment="Center" FontWeight="Bold"/>

            <Button x:Name="btnOk" Content="Ok" Click="BtnOk_Click" Foreground="White"  Grid.Row="1" Width="50" Background="#142128" Opacity="0.5" BorderBrush="Black"/>
        </Grid>
    </Border>
    
</Window>
